# 哨兵服务使用的端口
port 26381
dir "/mnt/d/code/python/weridolin.github.io/docs/redis/sentinel"
# sentinel monitor <master-name> <ip> <redis-port> <quorum>
# 让 sentinel 去监控一个地址为 ip:port 的主服务器，这里的 master-name 可以自定义；
# <quorum> 是一个数字，表示当有多少个 sentinel 认为主服务器宕机时，它才算真正的宕机掉，通常数量为半数或半数以上才会认为主机已经宕机，<quorum> 需要根据 sentinel 的数量设置。
sentinel monitor test1 127.0.0.1 7002 2

# 哨兵服务使用的端口

# sentinel monitor <master-name> <ip> <redis-port> <quorum>
# 让 sentinel 去监控一个地址为 ip:port 的主服务器，这里的 master-name 可以自定义；
# <quorum> 是一个数字，表示当有多少个 sentinel 认为主服务器宕机时，它才算真正的宕机掉，通常数量为半数或半数以上才会认为主机已经宕机，<quorum> 需要根据 sentinel 的数量设置。
sentinel monitor test2 127.0.0.1 7000 1

# 哨兵服务使用的端口

# sentinel monitor <master-name> <ip> <redis-port> <quorum>
# 让 sentinel 去监控一个地址为 ip:port 的主服务器，这里的 master-name 可以自定义；
# <quorum> 是一个数字，表示当有多少个 sentinel 认为主服务器宕机时，它才算真正的宕机掉，通常数量为半数或半数以上才会认为主机已经宕机，<quorum> 需要根据 sentinel 的数量设置。
sentinel monitor test3 127.0.0.1 7004 3

# Generated by CONFIG REWRITE
protected-mode no
latency-tracking-info-percentiles 50 99 99.9
user default on nopass sanitize-payload ~* &* +@all
sentinel myid fb1834603f3a1ec75bbd8fdd881dc80cac39dd77
sentinel config-epoch test1 0
sentinel leader-epoch test1 0
sentinel config-epoch test2 2
sentinel leader-epoch test2 2
sentinel config-epoch test3 0
sentinel leader-epoch test3 0
sentinel current-epoch 2

sentinel known-replica test1 127.0.0.1 7005

sentinel known-replica test2 127.0.0.1 7003

sentinel known-replica test3 127.0.0.1 7001

